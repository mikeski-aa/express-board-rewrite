extends layout

block content
  div(class="mainCont")
    h1= title
    
    ul 
      each message in messages 
        li(class="message")
          p #{message.username} - Added on #{message.date} 
          p #{message.message} 

